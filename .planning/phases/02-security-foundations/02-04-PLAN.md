---
phase: 02-security-foundations
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - Enterprise/Maskings/AttributeMasking.h
  - Enterprise/Maskings/AttributeMasking.cpp
  - Enterprise/Maskings/AttributeMaskingEE.h
  - Enterprise/Maskings/AttributeMaskingEE.cpp
  - Enterprise/Ssl/SslServerFeatureEE.h
  - Enterprise/Ssl/SslServerFeatureEE.cpp
  - tests/unit/MaskingStrategiesTest.cpp
  - tests/unit/SslServerFeatureEETest.cpp
  - tests/mocks/SslMocks.h
  - tests/CMakeLists.txt
  - CMakeLists.txt
autonomous: true
requirements: [MASK-01, MASK-02, MASK-03, SSL-01, SSL-02, SSL-03]

must_haves:
  truths:
    - "AttributeMasking applies field-level masking rules to query results"
    - "Masking rules are configurable per collection and per user role"
    - "Built-in masking strategies redact, hash, partial, and randomize data correctly"
    - "SslServerFeatureEE extends base SSL with enterprise TLS options"
    - "mTLS client certificate validation works"
    - "TLS version and cipher suite restrictions are configurable and enforced"
  artifacts:
    - path: "Enterprise/Maskings/AttributeMaskingEE.h"
      provides: "InstallMaskingsEE() registration for enterprise strategies"
      contains: "InstallMaskingsEE"
    - path: "Enterprise/Maskings/AttributeMaskingEE.cpp"
      provides: "Enterprise masking strategy implementations"
      min_lines: 60
    - path: "Enterprise/Maskings/AttributeMasking.h"
      provides: "Field-level masking with per-collection/per-role config"
      contains: "class AttributeMasking"
    - path: "Enterprise/Ssl/SslServerFeatureEE.h"
      provides: "Enterprise SSL overrides for mTLS and cipher restrictions"
      contains: "class SslServerFeatureEE"
    - path: "Enterprise/Ssl/SslServerFeatureEE.cpp"
      provides: "verifySslOptions, createSslContexts, dumpTLSData overrides"
      min_lines: 80
    - path: "tests/unit/MaskingStrategiesTest.cpp"
      provides: "Tests for MASK-01 through MASK-03"
      min_lines: 60
    - path: "tests/unit/SslServerFeatureEETest.cpp"
      provides: "Tests for SSL-01 through SSL-03"
      min_lines: 60
  key_links:
    - from: "Enterprise/Maskings/AttributeMaskingEE.cpp"
      to: "AttributeMasking::installMasking()"
      via: "InstallMaskingsEE registers enterprise strategies in base framework"
      pattern: "installMasking|InstallMaskingsEE"
    - from: "Enterprise/Ssl/SslServerFeatureEE.cpp"
      to: "OpenSSL SSL_CTX"
      via: "createSslContexts configures mTLS and cipher restrictions"
      pattern: "SSL_CTX_set_verify|SSL_VERIFY_PEER|SSL_CTX_set_cipher_list"
    - from: "Enterprise/Ssl/SslServerFeatureEE.cpp"
      to: "SslServerFeature base class"
      via: "MUST call base collectOptions/validateOptions (Pitfall 6)"
      pattern: "SslServerFeature::collectOptions|SslServerFeature::validateOptions"
---

<objective>
Implement Data Masking strategies and Enhanced SSL/TLS (mTLS + cipher restrictions)

Purpose: Data Masking extends ArangoDB's existing masking framework with enterprise strategies (xifyFront, email, creditCard, phone) used by arangodump. Enhanced SSL/TLS adds mTLS client certificate authentication, TLS version enforcement, and cipher suite restrictions to SslServerFeatureEE. Both are the smallest Phase 2 modules and combine naturally into one plan.

Output: Enterprise masking strategies registered via InstallMaskingsEE(), SslServerFeatureEE with 3 virtual method overrides, and unit tests for both.
</objective>

<execution_context>
@/Users/aarjay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aarjay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-security-foundations/02-RESEARCH.md

@Enterprise/Maskings/AttributeMasking.h
@Enterprise/Maskings/AttributeMasking.cpp
@Enterprise/Maskings/AttributeMaskingEE.h
@Enterprise/Ssl/SslServerFeatureEE.h
@Enterprise/Ssl/SslServerFeatureEE.cpp
@CMakeLists.txt
@tests/CMakeLists.txt

<interfaces>
<!-- Existing stubs to be replaced -->

From Enterprise/Maskings/AttributeMaskingEE.h (current stub - REPLACE):
```cpp
namespace arangodb {
// TODO: Implement AttributeMaskingEE (Phase 4)
}
```

From Enterprise/Ssl/SslServerFeatureEE.h (current stub - EXPAND):
```cpp
class SslServerFeatureEE final : public SslServerFeature {
 public:
  static constexpr std::string_view name() noexcept { return "SslServer"; }
  explicit SslServerFeatureEE(application_features::ApplicationServer& server);
};
```

Base masking framework (from lib/Maskings/):
```cpp
// AttributeMasking::installMasking(name, factoryFn) registers a named strategy
// InstallMaskings() registers base strategies (randomString, random)
// InstallMaskingsEE() registers enterprise strategies (called after InstallMaskings)
```

SslServerFeature available virtual overrides (prepare/unprepare are FINAL):
- verifySslOptions()
- createSslContexts() -> SslContextList
- dumpTLSData(VPackBuilder&) -> Result
- collectOptions(shared_ptr<ProgramOptions>)
- validateOptions(shared_ptr<ProgramOptions>)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement enterprise masking strategies and InstallMaskingsEE</name>
  <files>
    Enterprise/Maskings/AttributeMasking.h,
    Enterprise/Maskings/AttributeMasking.cpp,
    Enterprise/Maskings/AttributeMaskingEE.h,
    Enterprise/Maskings/AttributeMaskingEE.cpp,
    tests/unit/MaskingStrategiesTest.cpp,
    tests/CMakeLists.txt,
    CMakeLists.txt
  </files>
  <behavior>
    - Test: InstallMaskingsEE() registers "xifyFront", "email", "creditCard", "phone" strategies
    - Test: xifyFront("John Doe") produces "xxxx xxx" (characters replaced with 'x', preserving length/spaces)
    - Test: email("user@example.com") produces hashed email in "AAAA.BBBB@CCCC.invalid" format
    - Test: creditCard("4111111111111111") produces "xxxxxxxxxxxx1111" (last 4 digits preserved)
    - Test: phone("+1-555-123-4567") produces "xxx-xxx-xxx-4567" (last 4 digits preserved)
    - Test: Masking config JSON with collection-specific rules is parsed correctly
    - Test: Per-role masking applies different rules based on user role
    - Test: Masking a VelocyPack document produces redacted output for configured fields
  </behavior>
  <action>
    1. **Update Enterprise/Maskings/AttributeMasking.h**: Expand beyond the current stub. Define the enterprise masking strategy classes:
       - `class XifyFrontMask` -- replaces characters with 'x', preserving structure (spaces, length). Factory: `static std::unique_ptr<MaskingFunction> create(VPackSlice config)`
       - `class EmailMask` -- hashes email into reproducible "AAAA.BBBB@CCCC.invalid" format
       - `class CreditCardMask` -- masks all but last 4 digits
       - `class PhoneMask` -- masks all but last 4 digits
       - Each class implements a `apply(VPackSlice input, VPackBuilder& output)` method
       - Also implement collection-level and role-level masking config parsing from JSON

    2. **Implement Enterprise/Maskings/AttributeMasking.cpp**: Full implementation of:
       - `AttributeMasking::apply()` -- takes a document, applies field-level rules based on collection and role config
       - `AttributeMasking::loadConfig(std::string const& jsonPath)` -- loads masking rules from JSON file (per --maskings option)
       - Config format: `{ "collections": { "collName": { "rules": [{ "path": "fieldName", "type": "xifyFront" }] } } }`
       - Per-role support: rules can include a `"roles"` array to restrict which user roles see masked data

    3. **Rewrite Enterprise/Maskings/AttributeMaskingEE.h**: Replace stub with proper declaration:
       ```cpp
       namespace arangodb::maskings {
       void InstallMaskingsEE();
       }
       ```

    4. **Create Enterprise/Maskings/AttributeMaskingEE.cpp**: Implement InstallMaskingsEE():
       ```cpp
       void InstallMaskingsEE() {
         AttributeMasking::installMasking("xifyFront", XifyFrontMask::create);
         AttributeMasking::installMasking("email", EmailMask::create);
         AttributeMasking::installMasking("creditCard", CreditCardMask::create);
         AttributeMasking::installMasking("phone", PhoneMask::create);
       }
       ```

    5. **Update CMakeLists.txt**: Add `Enterprise/Maskings/AttributeMaskingEE.cpp` to sources.

    6. **Create tests/unit/MaskingStrategiesTest.cpp**: Tests per behavior block. Test each strategy with known inputs/outputs. Test config loading from JSON string. Test per-collection and per-role rule application.

    7. **Update tests/CMakeLists.txt**: Add `masking_strategies_test` executable.
  </action>
  <verify>
    <automated>cd /Users/aarjay/Documents/OpenArangoDBCore/build && cmake .. -DBUILD_TESTING=ON && cmake --build . --target masking_strategies_test && ctest -R masking_strategies_test --output-on-failure</automated>
  </verify>
  <done>
    - InstallMaskingsEE() registers 4 enterprise masking strategies
    - xifyFront, email, creditCard, phone produce correct masked output
    - Masking config supports per-collection and per-role rules
    - JSON config parsing works for --maskings option
    - All masking_strategies_test tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement SslServerFeatureEE virtual overrides for mTLS and cipher restrictions</name>
  <files>
    Enterprise/Ssl/SslServerFeatureEE.h,
    Enterprise/Ssl/SslServerFeatureEE.cpp,
    tests/unit/SslServerFeatureEETest.cpp,
    tests/mocks/SslMocks.h,
    tests/CMakeLists.txt
  </files>
  <behavior>
    - Test: SslServerFeatureEE::name() returns "SslServer"
    - Test: collectOptions adds --ssl.require-client-cert, --ssl.min-tls-version, --ssl.enterprise-cipher-suites
    - Test: collectOptions calls base SslServerFeature::collectOptions first (Pitfall 6)
    - Test: verifySslOptions rejects TLS version below configured minimum
    - Test: createSslContexts with require-client-cert enables SSL_VERIFY_PEER | SSL_VERIFY_FAIL_IF_NO_PEER_CERT
    - Test: createSslContexts applies cipher suite allowlist
    - Test: dumpTLSData includes enterprise TLS fields in VPack output
    - Test: SslServerFeatureEE is constructible (static_assert non-abstract)
  </behavior>
  <action>
    1. **Rewrite Enterprise/Ssl/SslServerFeatureEE.h**: Expand the current stub per Pattern 4 from research:
       - Add virtual method overrides: `verifySslOptions()`, `createSslContexts()`, `dumpTLSData(VPackBuilder&)`
       - Add `collectOptions()` and `validateOptions()` overrides
       - Add private members: `_requireClientCert` (bool), `_minTlsVersion` (uint64_t), `_allowedCipherSuites` (string)
       - Keep `static constexpr std::string_view name() noexcept { return "SslServer"; }`

    2. **Implement Enterprise/Ssl/SslServerFeatureEE.cpp**:
       - `collectOptions()`: MUST call `SslServerFeature::collectOptions(opts)` first (Pitfall 6!). Then add enterprise options: `--ssl.require-client-cert` (bool, default false), `--ssl.min-tls-version` (string, default "1.2"), `--ssl.enterprise-cipher-suites` (string, allowlist).
       - `validateOptions()`: MUST call `SslServerFeature::validateOptions(opts)` first. Then validate min TLS version is "1.2" or "1.3". Validate cipher suite string is non-empty if set.
       - `verifySslOptions()`: Check that configured TLS version is at least the minimum. Reject TLS 1.0 and 1.1. Call base verifySslOptions if it exists.
       - `createSslContexts()`: Create SSL contexts. If `_requireClientCert` is true, call `SSL_CTX_set_verify(ctx, SSL_VERIFY_PEER | SSL_VERIFY_FAIL_IF_NO_PEER_CERT, nullptr)` and load CA cert file for client verification. If `_allowedCipherSuites` is set, call `SSL_CTX_set_cipher_list(ctx, _allowedCipherSuites.c_str())`. For TLS 1.3, use `SSL_CTX_set_ciphersuites()`. Enforce min TLS version via `SSL_CTX_set_min_proto_version()`.
       - `dumpTLSData()`: Add enterprise fields to VPack builder: requireClientCert, minTlsVersion, allowedCipherSuites.

    3. **Create tests/mocks/SslMocks.h**: Mock SSL_CTX operations. Since we can't easily mock OpenSSL C functions, test at the higher level: verify that SslServerFeatureEE's option parsing and validation work correctly. For createSslContexts, test with a real SSL_CTX if OpenSSL is available, or mock the context creation.

    4. **Create tests/unit/SslServerFeatureEETest.cpp**: Tests per behavior block. Focus on:
       - Option registration (collectOptions adds the 3 enterprise options)
       - Base class call chain (collectOptions calls base first)
       - Validation logic (rejects TLS < 1.2)
       - dumpTLSData produces expected VPack
       - static_assert non-abstract

    5. **Update tests/CMakeLists.txt**: Add `ssl_ee_feature_test` executable linking `OpenSSL::SSL`.
  </action>
  <verify>
    <automated>cd /Users/aarjay/Documents/OpenArangoDBCore/build && cmake --build . --target ssl_ee_feature_test && ctest -R ssl_ee_feature_test --output-on-failure</automated>
  </verify>
  <done>
    - SslServerFeatureEE overrides verifySslOptions, createSslContexts, dumpTLSData
    - mTLS enforcement via SSL_VERIFY_PEER | SSL_VERIFY_FAIL_IF_NO_PEER_CERT
    - TLS version minimum enforcement (rejects < TLS 1.2)
    - Cipher suite allowlist applied
    - Base class collectOptions/validateOptions called first (Pitfall 6)
    - static_assert confirms non-abstract
    - All ssl_ee_feature_test tests pass
  </done>
</task>

</tasks>

<verification>
- `cd build && ctest -R "masking|ssl_ee" --output-on-failure` -- all masking and SSL tests green
- `grep "InstallMaskingsEE\|installMasking" Enterprise/Maskings/AttributeMaskingEE.cpp` confirms strategy registration
- `grep "SslServerFeature::collectOptions" Enterprise/Ssl/SslServerFeatureEE.cpp` confirms base class call chain
- `grep "SSL_VERIFY_PEER" Enterprise/Ssl/SslServerFeatureEE.cpp` confirms mTLS configuration
</verification>

<success_criteria>
- InstallMaskingsEE() registers 4 enterprise masking strategies (xifyFront, email, creditCard, phone)
- Masking rules configurable per collection and per role
- SslServerFeatureEE overrides 3 virtual methods for enterprise TLS
- mTLS client certificate validation configurable
- TLS version and cipher suite restrictions enforced
- All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-security-foundations/02-04-SUMMARY.md`
</output>
